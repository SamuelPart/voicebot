<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      PERMISOS
      Aló NO pide INTERNET en el MVP: el procesamiento es 100 % on-device.
      Si más adelante activas el backend opcional, añade:
          <uses-permission android:name="android.permission.INTERNET" />
      y documenta la finalidad en la política de privacidad + formulario Data Safety.
    -->

    <!-- Vibración al detectar un caso que necesita revisión (sin audio). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".AloApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alo"
        tools:targetApi="34">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Servicio de escucha de notificaciones.
          El atributo permission=BIND_NOTIFICATION_LISTENER_SERVICE es de nivel firma:
          solo el sistema puede enlazarlo, por eso exportarlo es seguro.
        -->
        <service
            android:name=".service.WaListenerService"
            android:exported="true"
            android:label="@string/listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
